# OS noise
.DS_Store
Thumbs.db

# Editor noise
.vscode/
*.swp

# Node (only present locally if you run the Tailwind rebuild step)
node_modules/
package-lock.json

# Never commit credentials
*credentials*
*secrets*
.env
.env.*
ftp-config.*
